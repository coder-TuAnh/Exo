@*@using ProjectLibrary.Config*@
@model ProjectLibrary.Database.MenuSystem
@{
    ViewBag.Title = "MenuSystem";
    Layout = "~/Areas/Administrator/Views/Templates/_LayoutPage.cshtml";
 }
<form action="@Url.Action("UpdateIndex", "MenuSystem")" method="POST" id="updateIndex">
     <div id="jtable-main-container"></div>
    <script type="text/javascript">
        $(document).ready(function() {
            $('#jtable-main-container').jtable({
                title: 'MenuSystem',
                paging: true, //Enable paging
                pageSize: 20, //Set page size (default: 10)
                sorting: true, //Enable sorting
                defaultSorting: 'Name ASC', //Set default sorting
                selecting: true, //Enable selecting
                multiselect: true, //Allow multiple selecting
                selectingCheckboxes: true, //Show checkboxes on first column
                columnResizable: true, //Actually, no need to set true since it's default
                columnSelectable: true, //Actually, no need to set true since it's default
                saveUserPreferences: true, //Actually, no need to set true since it's default
            
                actions: {
                    listAction: '@Url.Action("ListMenu", "MenuSystem")',
                    @*deleteAction: '@Url.Action("Delete", "MenuSystem")',*@
                },
                fields: {
                    Id: {
                        key: true,
                        create: false,
                        edit: false,
                        list: false
                    },
                    Name: {
                        title: 'Name',
                        width: '20%',
                       display: function (data) {
                        return '<span class="gach-' + data.record.Level + '">' + data.record.Name + '</span>';
                        },
                    },
                    
                    '':
                        {
                            title: '',
                            width: '2%',
                            sorting: false,
                            display: function(data) {
                                return '<button title="Edit" type="button" class="jtable-command-button jtable-edit-command-button" onclick="editRecord(' + data.record.ID + ')"><span>Edit</span></button>';
                            }
                        }
                },
            });
            $('#jtable-main-container').jtable('load');
        });
    </script>
</form>
@*<script>
    function editRecord(menuid) {
        window.location = '@Url.Action("Update","Menu")/' + menuid + "?location=@menuLocation.AliasMenu";
    }
</script>*@




